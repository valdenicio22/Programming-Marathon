#include <bits/stdc++.h>

using namespace std;

int main(){

    int x, y;
    cin >> x >> y;
    int vx[x], vy[y];
    for (int i=0;i<x;i++)
        cin >> vx[i];
    for (int i=0;i<y;i++)
        cin >> vy[i];
    int maior = max(x,y)


return 0;
}
